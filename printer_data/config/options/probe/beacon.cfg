[beacon]
serial: /dev/serial/by-id/usb-Beacon_Beacon_RevH_16F3C5915157355957202020FF0E0B12-if00
x_offset: 0              # update with offset from nozzle on your machine
y_offset: 19.25          #30.50          # update with offset from nozzle on your machine
mesh_main_direction: x
mesh_runs: 2
#default_probe_method: proximity|contact


#[safe_z_home]
#home_xy_position: 175, 175 # update for your machine
#z_hop: 3

[bed_mesh]
zero_reference_position: 175, 175 # The centre of your bed. 
speed: 150
horizontal_move_z: 3    #3
mesh_min: 20,50         # The first probed coordinate, nearest to the origin. This coordinate is relative to the probe's location.
mesh_max: 330,330       # The probed coordinate farthest from the origin. This is not necessarily the last point probed, as the probing process occurs in a zig-zag fashion. As with mesh_min, this coordinate is relative to the probe's location.
mesh_pps: 0,0           # Disable interpolation - mesh is probably dense enough
probe_count: 25,25
adaptive_margin: 10
algorithm: bicubic
#bicubic_tension: 0.2
#split_delta_z: .010
scan_overshoot: 5


[homing_override]
gcode:
  # Move 5 up, just in case (this is our safety zhop, this needs 'set_position_z: 0' below)
  G91 ; set relative positioning
  G0 Z5 F1000 ; 5 up zhop
  {% if not rawparams or 'Y' in rawparams %}
    {action_respond_info('Homing Y')}
    G28 Y
    G90 ; set absolute positioning
    G0 Y175 F6000 ; return to center
    M400 ; Wait for move to finish
  {% endif %}
  {% if not rawparams or 'X' in rawparams %}
    {action_respond_info('Homing X')}
    G28 X
    G90 ; set absolute positioning
    G0 X175 F6000 ; return to center
    M400 ; Wait for move to finish
  {% endif %}
  {% if not rawparams or 'Z' in rawparams %}
    {action_respond_info('Homing Z')}
    G90 ; set absolute positioning
    G0 X175 Y175 F6000 ; return to center, please add your offsets manually if you want to
    G28 Z
    G0 Z10 F1000 ; move to z10 before probe
    G28 Z METHOD=CONTACT CALIBRATE=1
    G0 Z5 F1000 ; move to z5 to finish
    M400 ; Wait for move to finish
  {% endif %}
  G90 ; set absolute positioning
axes: xyz
set_position_z: 0 # This forces the z position to be at 0 when we start homing, so we can move the Z up before homing.